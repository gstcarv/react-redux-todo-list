import React from 'react';
import { Navbar } from 'react-bootstrap'

const AppNavbar = () => (
  <Navbar bg="dark">
    <Navbar.Brand href="#home" className="text-white">Todo List</Navbar.Brand>
  </Navbar>
);

export default AppNavbar;
